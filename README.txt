SquackTS Enterprise (Tkinter + SQLite)

Executar:
1) python -m venv venv
2) venv\Scripts\python -m pip install -r requirements.txt
3) venv\Scripts\python app.py

Notas:
- Base de dados em %LOCALAPPDATA%\SquackTS_Enterprise\data
- Backups em ./backups
